#include <stdio.h>

/**
 * main - printf to print new line with a proper grammer
 * Return: 0 if sucess one else
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
