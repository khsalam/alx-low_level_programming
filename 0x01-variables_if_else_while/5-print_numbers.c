#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print the slphabit charactrers using putchar
 * Return: always return 0
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		printf("%d", c);
	}
	printf("\n");
	return (0);
}
