#include "main.h"
#include "ctype.h"

/**
 * _ialpha - check if input is alpha 
 * c: the Input paramater
 */
int print_sign(int c)
{
	if (c > 0)
		return (1);
	if (c == 0)
		return (0);
	if (c < 0)
	       return(-1);
}
