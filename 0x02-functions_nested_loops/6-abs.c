#include "main.h"
#include "ctype.h"

/**
 *_abs(int) - check if input is alpha
 *@c: the Input paramater
 *
 * Return: 1 for positive -1 for negative else 0
 */
int _abs(int c)
{
	int r;

	r = abs(c);
	return (r);
}
