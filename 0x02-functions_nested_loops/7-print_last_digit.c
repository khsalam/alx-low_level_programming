#include "main.h"
#include "ctype.h"

/**
 *print_last_digit - check if input is alpha
 *@c: the Input paramater
 *
 * Return: last digit
 */
int print_last_digit(int c)
{
	int r;

	r = c % 10;
	return (r);
}
